import React from 'react';
import ReactDOM from 'react-dom';

document.addEventListener('DOMContentLoaded', () => {
    ReactDOM.render(<div>Pusheen's Snake App!</div>, document.getElementById('root'));
});